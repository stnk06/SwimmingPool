<UserControl x:Class="SwimmingPool.Views.ReportsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:mah="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900"
             Language="ru-RU">
    <Border Style="{StaticResource ContentCardStyle}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="0 0 0 15">
                <TextBlock Text="Выручка" FontSize="24" FontWeight="Light"/>
                <TextBlock Text="Расчет выручки бассейна за определенный период" Foreground="{DynamicResource MahApps.Brushes.Gray3}" Margin="0 5 0 0"/>
            </StackPanel>

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="2*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <mah:Tile Title="Всего клиентов" TiltFactor="2" Margin="5" Background="{DynamicResource MahApps.Brushes.Accent}">
                        <TextBlock Text="{Binding TotalClients}" FontSize="48" FontWeight="Bold"/>
                    </mah:Tile>
                    <mah:Tile Title="Активных абонементов" TiltFactor="2" Margin="5" Background="{DynamicResource MahApps.Brushes.Accent2}">
                        <TextBlock Text="{Binding ActiveMemberships}" FontSize="48" FontWeight="Bold"/>
                    </mah:Tile>
                </StackPanel>

                <GridSplitter Grid.Column="1" Width="10" Background="Transparent"/>

                <GroupBox Grid.Column="2" Header="Отчет о доходах за период" Style="{DynamicResource MahApps.Styles.GroupBox}" mah:HeaderedControlHelper.HeaderFontSize="16">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <StackPanel Orientation="Horizontal" Margin="5">
                            <DatePicker mah:TextBoxHelper.Watermark="С" SelectedDate="{Binding StartDate}" Margin="0 0 10 0"/>
                            <DatePicker mah:TextBoxHelper.Watermark="По" SelectedDate="{Binding EndDate}" Margin="0 0 10 0"/>
                            <Button Content="Сформировать" Command="{Binding GenerateReportCommand}" Style="{StaticResource AccentButtonStyle}" Margin="0 0 10 0"/>

                            <!-- Кнопка экспорта -->
                            <Button Command="{Binding ExportReportCommand}" ToolTip="Экспорт в PDF">
                                <StackPanel Orientation="Horizontal">
                                    <iconPacks:PackIconMaterial Kind="FilePdfBox" Margin="0 0 5 0"/>
                                    <TextBlock Text="PDF"/>
                                </StackPanel>
                            </Button>
                        </StackPanel>

                        <DataGrid Grid.Row="1" ItemsSource="{Binding RevenueReportItems}" Margin="5" AutoGenerateColumns="False" IsReadOnly="True">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Тип абонемента" Binding="{Binding MembershipType}" Width="*"/>
                                <DataGridTextColumn Header="Продано (шт)" Binding="{Binding SalesCount}" Width="Auto"/>
                                <DataGridTextColumn Header="Сумма" Binding="{Binding TotalRevenue, StringFormat='C'}" Width="Auto"/>
                            </DataGrid.Columns>
                        </DataGrid>

                        <TextBlock Grid.Row="2" Text="{Binding TotalPeriodRevenue, StringFormat='Итого: {0:C}'}" Margin="5" HorizontalAlignment="Right" FontWeight="Bold" FontSize="16"/>
                    </Grid>
                </GroupBox>
            </Grid>
        </Grid>
    </Border>
</UserControl>