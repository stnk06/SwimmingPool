<UserControl x:Class="SwimmingPool.Views.MembershipTypesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900"
             Language="ru-RU">
    <Border Style="{StaticResource ContentCardStyle}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="0 0 0 15">
                <TextBlock Text="СПРАВОЧНИК: ТИПЫ АБОНЕМЕНТОВ" FontSize="24" FontWeight="Light"/>
                <TextBlock Text="Управление видами и стоимостью абонементов" Foreground="{DynamicResource MahApps.Brushes.Gray3}" Margin="0 5 0 0"/>
            </StackPanel>

            <Grid Grid.Row="1" Margin="0 0 0 10">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">

                    <Button Command="{Binding ExportExcelCommand}" Margin="0,0,10,0"  ToolTip="Экспорт в Excel (CSV)">
                        <StackPanel Orientation="Horizontal">
                            <iconPacks:PackIconMaterial Kind="FileExcel" Margin="0,0,5,0" Foreground="Green"/>
                            <TextBlock Text="Экспорт"/>
                        </StackPanel>
                    </Button>

                    <Button Command="{Binding AddCommand}" Style="{StaticResource AccentButtonStyle}">
                        <StackPanel Orientation="Horizontal">
                            <iconPacks:PackIconMaterial Kind="Plus" Margin="0 0 5 0"/>
                            <TextBlock Text="Добавить тип"/>
                        </StackPanel>
                    </Button>
                    <Button Command="{Binding EditCommand}" Margin="10 0 0 0">
                        <StackPanel Orientation="Horizontal">
                            <iconPacks:PackIconMaterial Kind="Pencil" Margin="0 0 5 0"/>
                            <TextBlock Text="Редактировать"/>
                        </StackPanel>
                    </Button>
                    <Button Command="{Binding DeleteCommand}" Margin="10 0 0 0">
                        <StackPanel Orientation="Horizontal">
                            <iconPacks:PackIconMaterial Kind="Delete" Margin="0 0 5 0"/>
                            <TextBlock Text="Удалить"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>

            <DataGrid Grid.Row="2" ItemsSource="{Binding MembershipTypes}" SelectedItem="{Binding SelectedMembershipType}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Название" Binding="{Binding TypeName}" Width="*"/>
                    <DataGridTextColumn Header="Описание" Binding="{Binding Description}" Width="2*"/>
                    <DataGridTextColumn Header="Цена" Binding="{Binding Price, StringFormat='C'}" Width="Auto"/>
                    <DataGridTextColumn Header="Длительность (дней)" Binding="{Binding DurationInDays}" Width="Auto"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Border>
</UserControl>