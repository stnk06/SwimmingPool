<UserControl x:Class="SwimmingPool.Views.TrainersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900">
    <Border Style="{StaticResource ContentCardStyle}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="0" Margin="0 0 0 15">
                <TextBlock Text="СПРАВОЧНИК: ТРЕНЕРЫ" FontSize="24" FontWeight="Light"/>
                <TextBlock Text="Управление данными о тренерском составе" Foreground="{DynamicResource MahApps.Brushes.Gray3}" Margin="0 5 0 0"/>
            </StackPanel>
            <Grid Grid.Row="1" Margin="0 0 0 10">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">

                    <Button Command="{Binding ExportExcelCommand}" Margin="0,0,10,0"  ToolTip="Экспорт в Excel (CSV)">
                        <StackPanel Orientation="Horizontal">
                            <iconPacks:PackIconMaterial Kind="FileExcel" Margin="0,0,5,0" Foreground="Green"/>
                            <TextBlock Text="Экспорт"/>
                        </StackPanel>
                    </Button>

                    <Button Command="{Binding AddCommand}" Style="{StaticResource AccentButtonStyle}">
                        <StackPanel Orientation="Horizontal">
                            <iconPacks:PackIconMaterial Kind="Plus" Margin="0 0 5 0"/>
                            <TextBlock Text="Добавить тренера"/>
                        </StackPanel>
                    </Button>
                    <Button Command="{Binding EditCommand}" Margin="10 0 0 0">
                        <StackPanel Orientation="Horizontal">
                            <iconPacks:PackIconMaterial Kind="Pencil" Margin="0 0 5 0"/>
                            <TextBlock Text="Редактировать"/>
                        </StackPanel>
                    </Button>
                    <Button Command="{Binding DeleteCommand}" Margin="10 0 0 0">
                        <StackPanel Orientation="Horizontal">
                            <iconPacks:PackIconMaterial Kind="Delete" Margin="0 0 5 0"/>
                            <TextBlock Text="Удалить"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>
            <DataGrid Grid.Row="2" ItemsSource="{Binding Trainers}" SelectedItem="{Binding SelectedTrainer}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ФИО" Binding="{Binding FullName}" Width="*"/>
                    <DataGridTextColumn Header="Специализация" Binding="{Binding Specialization}" Width="*"/>
                    <DataGridTextColumn Header="Опыт (лет)" Binding="{Binding ExperienceYears}" Width="Auto"/>
                    <DataGridTextColumn Header="Контактная информация" Binding="{Binding ContactInfo}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Border>
</UserControl>